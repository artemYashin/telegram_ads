import { InputFile } from "grammy";
import { getBotInstance } from "../BotManager/Bot.js"

export async function editMessageMedia(chatid: number, msgid: number, file_id: string) {
    // const bot = getBotInstance();
    // return bot.api.editMessageMedia(chatid, msgid, new InputMedia(file_id));
}